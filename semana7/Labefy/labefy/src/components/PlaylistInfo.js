/* lista todas as playlists e exclui */
import React from "react";

class PlaylistInfo extends React.Component {
  state = {
    playlists: [],
  };

  /* FALTA OS DETALHES DA PLAYLIST */
  /* FALTA: ENTENDER COMO ELE PASSOU AS MUSICAS PARA A PLAYLIST, USANDO O ID?
  COMO ELE VAI FAZER ISSO USANDO A API. */
  render() {
    return (
      <>
        <h1>oi sou informações da playlist</h1>
      </>
    );
  }
}
export default PlaylistInfo;
