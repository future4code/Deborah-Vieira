/*function metodo(array) {
  let resultadoA = 0;
  let resultadoB = 1;
  let arrayFinal = [];

  for (let x of array) {
    resultadoA += x;
    resultadoB *= x;
  }

  arrayFinal.push(resultadoA);
  arrayFinal.push(resultadoB);
  return arrayFinal;
}

/*R: Ela recebe valores de resultadoA, oonde serão somados +1 ao resultado e 
Ela recebe valores de resultadoB, oonde serão multiplicados *1 ao resultado final,
e por fim, ela popula o array com os resultados de a e b.
Nome: popularArray */
